import {getMerchant} from '../../utils/mock'; Page({data:{products:getMerchant(1)!.products},toggle(e:WechatMiniprogram.BaseEvent){const products=this.data.products.map(p=>p.id===e.currentTarget.dataset.id?{...p,status:p.status===false}:p);this.setData({products})},goOrders(){wx.navigateTo({url:'/pages/merchant-orders/index'})},goSettings(){wx.navigateTo({url:'/pages/merchant-settings/index'})}})
